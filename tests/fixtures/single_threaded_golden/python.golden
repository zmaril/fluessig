//! GENERATED by fluessig bindgen from single_threaded_demo (api layer). Do not edit.
#![allow(clippy::all)]

use pyo3::exceptions::PyRuntimeError;
use pyo3::exceptions::PyStopAsyncIteration;
use pyo3::prelude::*;
use std::sync::Arc;
use std::time::Duration;
// The shared streaming contract — Poll/PollStream live in the fluessig-runtime crate.
use fluessig_runtime::{Poll, PollStream};

fn err(e: impl std::fmt::Display) -> PyErr {
    PyRuntimeError::new_err(e.to_string())
}

/// Register every generated class + function on the `#[pymodule]`.
pub(crate) fn register(m: &Bound<'_, PyModule>) -> PyResult<()> {
    Ok(())
}
// interface `Tui` is #[fluessig(single_threaded)] (a thread-confined `!Send` handle) — not supported by the python backend (node-only today); no binding emitted.
